[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npx prisma generate || true"

[deploy]
startCommand = "cd backend && npx prisma generate && npx prisma migrate deploy && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
